version: "2"
services:
  # builder:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   image:
  # release:
  #   ports:
  #     - 80:4000
  #     - 443:4001
  #   environment:
  #     - PG_USERNAME=postgres
  #     - PG_PASSWORD=postgres
  #     - PG_HOSTNAME=postgres
  #     - PG_DATABASE=ticket_prod
  #   image:
  #   depends_on:
  #     - postgres
  postgres:
    image: postgres:10
    restart: always
    volumes:
      - db-data:/var/lib/postgresql/data/
    ports:
      - 5432:5432

volumes:
  db-data:
